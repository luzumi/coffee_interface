@extends('admin.admin')

@section('dashboard')

{{--        <form method="GET" action="{{ route('logout') }}">--}}

{{--            <button type="submit" >--}}
                <div class="admin-help-image"></div>
{{--            </button>--}}
{{--        </form>--}}

@endsection
